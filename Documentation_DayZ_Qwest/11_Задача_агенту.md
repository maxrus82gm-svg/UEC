# ЗАДАЧА АГЕНТУ

Это рабочий shell-документ текущего агентского цикла.

Он нужен для того, чтобы хранить:

- текущую задачу в `БЛОКЕ 1`;
- последний `AGENT REPORT` в `БЛОКЕ 2`;
- последний `REVIEW / STATUS` в `БЛОКЕ 3`;
- короткую карту чтения `SplitDoc`.

Правила:

- Постоянные правила: `Documentation_DayZ_Qwest/12_Старт_агента.md`
- Тематические правила: `Documentation_DayZ_Qwest/20_SplitDoc` по матрице ниже

## Что читать всегда

Агент всегда читает:

- `Documentation_DayZ_Qwest/11_Задача_агенту.md`
- `Documentation_DayZ_Qwest/12_Старт_агента.md`

## Матрица чтения SplitDoc

- `UI / QuestMenu / QuestJournal` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_UI_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Quest Editor / JSON_Quvest` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_EDITOR_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `JSON contract / quest logic` -> `Documentation_DayZ_Qwest/20_SplitDoc/QUEST_JSON_CONTRACT.md` + `Documentation_DayZ_Qwest/21_Project_Docs/QUEST_LOGIC_SPEC.md`
- `DayZ layout viewer` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_LAYOUT_VIEWER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `RPC / client-server sync` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_RPC_SYNC_RULES.md`
- `Git / REVIEW / commit / push check / история Git` -> `Documentation_DayZ_Qwest/20_SplitDoc/GIT_REVIEW_RULES.md`
- `Documentation / Markdown / создание документов / обновление правил` -> `Documentation_DayZ_Qwest/20_SplitDoc/DOCUMENTATION_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Encoding / кириллица / восстановление русского текста / кракозябры` -> `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `Server profile / dev JSON / перенос на сервер` -> `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md`
- `Trader / TraderObjects / TraderConfig` -> `Documentation_DayZ_Qwest/20_SplitDoc/TRADER_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/SERVER_PROFILE_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`
- `History / status / прошлые решения` -> `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
- `DayZ .c / Enforce Script / runtime compile` -> `Documentation_DayZ_Qwest/20_SplitDoc/DAYZ_ENFORCE_SCRIPT_RULES.md` + `Documentation_DayZ_Qwest/20_SplitDoc/ENCODING_RULES.md`

## Канонические пути

Тематические правила читать из:

- `Documentation_DayZ_Qwest/20_SplitDoc`

Проектные документы читать из:

- `Documentation_DayZ_Qwest/21_Project_Docs`

## Очистка выполненной задачи

После успешного выполнения задачи агент обязан:

- обновить `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`;
- при необходимости обновить `БЛОК 3 — REVIEW / STATUS`;
- добавить запись в `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`;
- очистить активный `БЛОК 1` в `Documentation_DayZ_Qwest/11_Задача_агенту.md`.

После очистки под заголовком `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не должно оставаться содержимого завершённой задачи.

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

================================================================================

# 🟩 БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AGENT REPORT

TASK:
- `TASK 154 — Базовая runtime-архитектура GROUP / EVENT / ACTIVATION и публичная точка входа`.

STATUS:
- `COMPLETED`.
- Реализован и собран базовый server-only orchestration-фундамент; runtime-сервер не запускался.

RULES PATH:
- Основные правила и SplitDoc: `P:\Silver_77_Quests\Documentation_DayZ_Qwest`.

JSON SCHEMA:
- Canonical `MigrationConfig.json` содержит корневые массивы `groups`, `events`, `activations`.
- GROUP хранит stable `groupId`, отдельное информационное `name` и все прежние migration-параметры.
- EVENT хранит `eventId`, `name` и `groupIds`.
- ACTIVATION хранит `activationId`, `name`, `type`, `targetType`, `targetId`.

CREATED IDS:
- GROUP: `MIGRATION_TEST_001`, `MIGRATION_TEST_002`.
- EVENT: `EVENT_GLOBAL_MIGRATION` → обе текущие GROUP.
- ACTIVATION: `ACT_STARTUP_GLOBAL_MIGRATION` и `ACT_EXTERNAL_GLOBAL_MIGRATION` → `EVENT_GLOBAL_MIGRATION`.

PUBLIC API:
- Создан `S77MigrateAPI` с server-side вызовами `Activate(activationId)`, `StartGroup(groupId)`, `StartEvent(eventId)`.
- Методы возвращают `bool`; причины отказа пишутся маркерами `NOT_READY`, `NOT_FOUND`, `DISABLED`, `INVALID_TARGET`, `NO_RUNNABLE_GROUPS`, `LAUNCH_BUSY`.
- Quest / Trigger / Admin должны в будущем использовать facade, а не внутренние методы manager.

STARTUP / REPEATED LAUNCH:
- `MissionServer.OnInit()` по-прежнему инициализирует manager, но фактический startup теперь разрешается через STARTUP ACTIVATION → EVENT → текущий launch batch.
- Архитектурный one-shot `m_EventStarted` удалён.
- После завершения preparation новый API-вызов разрешён и получает новые `runtimeGroupId`; старые живые runtime-группы продолжают существовать.
- Очередь, cooldown, limits и persistence не добавлялись. Во время delay/weather preparation запрос возвращает `false` с `LAUNCH_BUSY`.

WEATHER BATCH:
- Weather decision считается только по runnable GROUP текущего запроса.
- При global `weatherEnabled=1` и хотя бы одном `weatherChangeEnabled=1` выполняется один transition, после которого запускаются только группы текущего batch.
- Прямой GROUP, EVENT и повторный запуск не подхватывают посторонние или ранее живущие группы.

BACKWARD COMPATIBILITY:
- Старый runtime JSON с `scenarios[]/scenarioId` продолжает загружаться.
- `scenarioId` используется как in-memory fallback `groupId`, отсутствующее `name` получает fallback.
- При отсутствии `events/activations` создаются in-memory `EVENT_GLOBAL_MIGRATION` и STARTUP/EXTERNAL activation, эквивалентные прежнему startup всех enabled legacy-групп.
- `$profile:Silver_77_Migrate/MigrationConfig.json` не переписывался и не редактировался.

VALIDATION:
- Проверяются непустые и уникальные `groupId`, `eventId`, `activationId`.
- Проверяются GROUP data, допустимые `STARTUP/EXTERNAL`, `GROUP/EVENT`, все EVENT/ACTIVATION references и duplicate group reference внутри EVENT.
- EVENT сначала разрешает все ссылки; неизвестный `groupId` не допускает частичный запуск. Disabled GROUP явно пропускается, пустой runnable batch отклоняется.
- Lookup по `name` отсутствует.

NOT IMPLEMENTED:
- Quest System к API не подключён.
- Scheduler/random, trigger-зоны, admin UI, cooldown/limits и persistence не реализованы.
- Migration AI, route, stuck/recovery/reverse, hold и lifetime mechanics не перерабатывались.

CHANGED FILES:
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateConfig.c`.
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateManager.c`.
- `Silver_77_Migrate_server/scripts/4_World/S77MigrateAPI.c` — новый public facade.
- `Silver_77_Migrate_server/config/MigrationConfig.json`.
- `Silver_77_Migrate_server/config/README_RU.md`.
- `Documentation_DayZ_Qwest/14_Квест_Миграция.md`.
- `Documentation_DayZ_Qwest/16_Идеи_Квеста_Миграции.md`.
- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`.
- `Documentation_DayZ_Qwest/11_Задача_агенту.md` — отчёт и очистка БЛОКА 1.

BUILD:
- `build_migrate_server.bat`: `PASS`, Addon Builder завершил `Build Successful`, wrapper exit code `0`.
- PBO: `P:\Mods_DONE\@Silver_77_Migrate_server\addons\Silver_77_Migrate_server.pbo`.
- PBO SHA-256: `3A0AC9F257E4F998D10F570ED1CC6CD66D9C3D06BF9FD07DB78A945116B0842C`.
- Подпись `.bisign` пересобрана.
- Support JSON байтово совпадает с canonical; SHA-256 обоих: `7FFC0C182AD83B180BD6F40F81469C1D9B2939D2014CFC5C9EE85586BD4CA9D1`.
- Сервер не запускался и не перезапускался.

CHECKS:
- Canonical JSON parse: `PASS`.
- GROUP/EVENT/ACTIVATION IDs и references: `PASS`.
- Старые gameplay-значения двух групп и координаты: `PASS`.
- Enforce formula-complexity review изменённых участков: `PASS`.
- Баланс фигурных скобок: `PASS`.
- UTF-8 без BOM и кириллица: `PASS`.
- `git diff --check`: `PASS`.

UNEXPECTED CHANGES:
- `.obsidian/workspace.json` изменился автоматически во время работы Obsidian; TASK 154 этот файл не редактировала и не откатывала.

BLOCK 1 CLEANUP CHECK:
- `PASS`.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ ОТЧЁТА
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

================================================================================

# 🟨 БЛОК 3 — REVIEW / STATUS
================================================================================

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## НАЧАЛО REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

TASK 101 REVIEW

Статус:
Принято после runtime-проверки пользователя.

Commit baseline:
`2fc3212f8a381efd7d33bdd60ff2491f5b5e3800`

Что изменено:
- QuestPanelBackdrop: 1040x620 -> 1040x710
- QuestPanel: 980x560 -> 980x650

Description:
- DescriptionPanel: 430x204 -> 430x272
- DescriptionScroll: 414x156 -> 414x224
- DescriptionText: 394x600 -> 406x224
- Scrollbar V: 1 -> 0

Dialog:
- DialogPanel: y 286 -> 358
- DialogPanel: 430x122 -> 430x164
- DialogScroll: 414x74 -> 414x116
- DialogText: 394x300 -> 406x116
- Scrollbar V: 1 -> 0

Buttons:
- AcceptButton y: 420 -> 540
- CompleteButton y: 420 -> 540
- CloseButton y: 490 -> 594

Подтверждено в runtime:
- текущий вариант визуально нормальный и пока принимается как рабочий baseline;
- `Description/Dialog` увеличены по вертикали;
- `Scrollbar V` отключён;
- используется простой стек `ScrollWidget -> MultilineTextWidget`;
- `QuestUI.c` не менялся;
- `wrapper/mask/SizeToChild/RichText/EditBox` не используются.

Known limitation:
- очень длинный текст может обрезаться, потому что scroll/clipping тема пока отложена.

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
## КОНЕЦ REVIEW
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

История задач вынесена в:

- `Documentation_DayZ_Qwest/20_SplitDoc/TASK_HISTORY.md`
